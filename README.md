# Atelier North — React only

This version does NOT use Vite.

It uses:
- React
- React DOM
- Create React App (`react-scripts`)
- Lucide React for icons

## Run

```bash
npm install
npm start
```

## Production build

```bash
npm run build
```

Edit company content in `src/index.js` and styling in `src/styles.css`.
